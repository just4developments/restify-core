// const projectService = require('./project.service');
// const rolesService = require('./role.service.js');

// var main = async () => {    
//     const projects = await projectService.find();
//     for(let p of projects) {
//         await projectService.fetchInCached(p._id, p);
//         await rolesService.fetchInCached(p._id);
//     }
// };

// main();